import React from "react";
import Button from "../Button";

export default class Form extends React.Component{
    render() {
        return (
            <form>
                <div>

                </div>
                <div>
                    <Button />
                </div>
            </form>
        )
    }
}